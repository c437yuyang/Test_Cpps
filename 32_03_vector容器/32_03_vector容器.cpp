// 32_03_vector容器.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"
#include <iostream>
#include <vector>
using namespace std;

int main()
{

#pragma region 结论:容量不够的时候，推入新的才会增加容量
	//vector<int> v;

	//cout << v.size() << "," << v.capacity() << endl; //0,0
	//v.push_back(1);
	//cout << v.size() << "," << v.capacity() << endl; //1,1
	//v.push_back(1);
	//cout << v.size() << "," << v.capacity() << endl; // 2,2
	//v.push_back(1);
	//cout << v.size() << "," << v.capacity() << endl;// 3,3
	//v.push_back(1);
	//cout << v.size() << "," << v.capacity() << endl;
	//v.push_back(1);
	//cout << v.size() << "," << v.capacity() << endl;

	//v.push_back(1);
	//cout << v.size() << "," << v.capacity() << endl;
	//v.push_back(1);
	//cout << v.size() << "," << v.capacity() << endl;
	//v.push_back(1);
	//cout << v.size() << "," << v.capacity() << endl;
#pragma endregion


	system("pause");
    return 0;
}

